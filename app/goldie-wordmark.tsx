export default function GoldieWordmark({className=""}:{className?:string}) {
  return <div className={`goldie-wordmark-lockup ${className}`.trim()} aria-label="Goldie Listing Factory">
    <span className="goldie-wordmark-name">Gold<span className="goldie-wordmark-i">ı<i>✦</i></span>e</span>
    <b>LISTING FACTORY</b>
  </div>;
}
