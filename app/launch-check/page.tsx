"use client";
import {useState} from "react";
export default function LaunchCheck(){const [result,setResult]=useState(""),[busy,setBusy]=useState(false);return <main style={{padding:40,maxWidth:720,margin:"auto"}}><h1>Owner email delivery check</h1><p>Sends a clearly labelled test only to your signed-in owner email. It does not start a subscription or create listings.</p><button disabled={busy} onClick={async()=>{setBusy(true);try{const r=await fetch("/api/launch-check",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({action:"send"})});setResult(JSON.stringify(await r.json()));}catch{setResult("Connection failed. Check email logs before retrying.");}finally{setBusy(false);}}}>Send my labelled test email</button><button disabled={busy} onClick={async()=>{setBusy(true);try{const r=await fetch("/api/launch-check",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({action:"busy"})});setResult(JSON.stringify(await r.json()));}catch{setResult("Busy recovery check failed.");}finally{setBusy(false);}}}>Check busy-service recovery without AI calls</button><pre style={{whiteSpace:"pre-wrap"}} role="status">{result}</pre></main>}
